#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    int t;
    cin>>t;
    for (int tc=1; tc<=t; ++tc) {
        cout << tc << ' ';
        string s1, s2, s12;
        cin>>s1>>s2>>s12;
        
    }

    return 0;
}